// Optional Supabase config. Leave empty to use local backend.
window.APP_CONFIG = {
    // If hosting backend elsewhere (Render/Railway/VPS), set e.g. 'https://seu-backend.onrender.com'
    API_BASE: '', // Será preenchido após deploy no Render
    // Optional Supabase config (unused if API_BASE is set). Leave empty to ignore
    SUPABASE_URL: '',
    SUPABASE_ANON_KEY: '',
};


